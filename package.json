{
  "name": "course-dag-builder",
  "private": true,
  "version": "0.1.0",
  "description": "Web app + API to import, edit, and visualize course prerequisite DAGs.",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev",
    "typecheck": "npm run -ws typecheck",
    "test": "npm run test:smoke && npm run test:grouping",
    "test:smoke": "npx tsc -b packages/parser-excel && node --test scripts/smoke.mjs",
    "test:grouping": "npx tsc -b packages/core packages/expression packages/parser-excel && node --test packages/core/test/*.mjs packages/expression/test/*.mjs packages/parser-excel/test/*.mjs",
    "lint": "echo \"Add eslint later\"",
    "format": "echo \"Add prettier later\""
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
